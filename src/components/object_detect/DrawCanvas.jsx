export default function DrawCanvas({}) {

    // canvas can be shown in any size, but the under real value is, so that any information interpolate from the canvas will be relative to that original size
    
    // will have mouse click functionality
    // click movement: first click -> drag -> release click = a bounding box with color

    // interpret drawn information
    // interpolate x,y,h of the drawn bbox, asign with currently chosing class (active POS button)

    // DrawCanvas just need to interpret list of bbox coordinates

    return (
        <>
            <canvas></canvas>
        </>

    )

}